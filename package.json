{
  "name": "flavor-bomb-app",
  "private": true,
  "workspaces": [
    "web",
    "mobile",
    "shared"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=web",
    "dev:mobile": "npm run start --workspace=mobile",
    "build:web": "npm run build --workspace=web",
    "type-check": "npm run type-check --workspace=web && npm run type-check --workspace=shared"
  }
}
