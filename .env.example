# Supabase — use your EXISTING project credentials (do not create a new project)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Expose the recipes schema in Supabase Dashboard → Settings → API → "Exposed schemas"
# Add "recipes" to the list alongside "public"
